@model IEnumerable<Med_Storage_Frontend.Models.TransferModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransferId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransferCreator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransferDestination)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransferStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransferDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransferId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransferCreator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransferDestination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransferStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransferDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TransferId }) |
                @Html.ActionLink("Details", "Details", new {id = item.TransferId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.TransferId })
            </td>
        </tr>
}
    </tbody>
</table>
